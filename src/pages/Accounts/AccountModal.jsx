import { Modal } from '@mui/material'
import React from 'react'

export const AccountModal = ( {open} ) => {
  return (
    <div>
        <Modal open={open}>
            
        </Modal>
    </div>
  )
}
