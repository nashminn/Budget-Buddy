import React from 'react'
import { Layout } from '../components/Layout'

export const Debts = ( {openSidebar} ) => {
  return (
    <Layout title="Debts" openSidebar={openSidebar}>Debts</Layout> 
  )
}
