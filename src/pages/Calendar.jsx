import React from 'react'
import { Layout } from '../components/Layout'

export const Calendar = ( {openSidebar} ) => {
  return (
    <Layout title="Calendar" openSidebar={openSidebar}>Calendar</Layout>
  )
}
